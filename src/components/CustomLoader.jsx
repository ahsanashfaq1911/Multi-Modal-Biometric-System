// import { CircularProgress, Backdrop } from "@mui/material";

// const CustomLoader = ({ open = true }) => {
//   return (
//     <Backdrop open={open} sx={{ zIndex: 1300, color: "#fff" }}>
//       <CircularProgress color="inherit" />
//     </Backdrop>
//   );
// };

// export default CustomLoader;
